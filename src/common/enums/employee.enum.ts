export enum EmployeeErrorMessage {
  EmployeesNotFound = 'Employees were not found',
  EmployeeNotFound = 'The employee was not found'
}

export enum EmployeeStatusEnum {
  Available = 'available',
  InProgress = 'in_progress',
  OutOfWork = 'out_of_work'
}
