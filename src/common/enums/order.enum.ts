export enum OrderStatus {
  New = 'new',
  InProgress = 'in_progress',
  Canceled = 'canceled',
  Completed = 'completed'
}

export enum OrderErrorMessage {
  OrdersNotFound = 'Orders were not found',
  OrderNotFound = 'The order was not found'
}
